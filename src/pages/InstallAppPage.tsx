import { useEffect } from "react";

const InstallAppPage = () => {
  useEffect(() => {
    window.location.replace("https://movienoirdownload.lovable.app");
  }, []);

  return null;
};

export default InstallAppPage;
